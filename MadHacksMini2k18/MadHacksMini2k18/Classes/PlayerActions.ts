module MadHacks {

    export class PlayerActions {
        actions: string;
        frames: number;

        constructor(actions: string, frames: number) {
            this.actions = actions;
            this.frames = frames;
        }
    }

}